fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
google-genai==2.8.0
Pillow==10.3.0
dateparser==1.2.0
structlog==24.2.0
slowapi==0.1.9
httpx==0.27.0
python-dotenv==1.0.1
# --- Phase 1: durable audit store + object storage -----------------------
# Async Postgres via SQLAlchemy 2.0 + asyncpg; Alembic for reversible migrations.
# google-cloud-storage for the GCS object store (prod). All three are imported
# lazily, so the test suite (in-memory backends) runs without them installed.
SQLAlchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
google-cloud-storage==2.16.0
# --- Phase 2: reused-image dedup (Redis pHash index) ---------------------
# Imported lazily; the test suite uses the in-memory index and never needs it.
redis==5.0.4
